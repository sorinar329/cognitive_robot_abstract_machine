"""Semantic maps initialization module.

This module serves as the initialization point for semantic maps in the
RoboKudo framework. It provides access to various semantic map implementations
for different environments and scenarios.

Available semantic maps include:

* IAI kitchen environment maps
* Region definitions
* Spatial relationships
* Frame transformations

.. note::
    Each semantic map is implemented as a separate module and can be
    imported individually based on the application requirements.
""" 